#include <stdio.h>
/**
 * main - Entry Poinnt for all codes
 *
 * Returns: Always 0 (Success)
 */
int main(void)
{
	int n;

	n = 48;
	while (n <58)
	{
		putchar(n);
		n++;
	}

	putchar(10);

	return (0);
}
