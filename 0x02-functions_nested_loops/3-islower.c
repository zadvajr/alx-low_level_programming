#include "main.h"
/**
 * _islower - function code that checks whether a character is lower or not
 *
 * @c: parameter to be used to store the arguments
 * Return: Always 1 or 0 depending on input (Succes)
 *
 */
int _islower(int c)
{
	if (c >= 97)
	{
		return (1);
	}

	else
	{
		return (0);
	}
}
