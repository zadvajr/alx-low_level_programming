#include "main.h"

/**
 * main - checks the code
 *
 * Return: Always return 0. on success.
 */
int main(void)
{
	int n;

	n = _strlen_recursion("Daniel Zadva Jnr is doing hard things with ALX SE");
	printf("%d\n", n);
	return (0);
}
